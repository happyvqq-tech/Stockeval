# 容器用的精簡相依（Dockerfile 讀這份，不是 requirements.txt）。
# 拿掉 pytest（測試才要）與 akshare（很重，且只有 A 股用得到），
# 免費方案的建置時間與映像檔大小差很多。

pandas>=2.0
pyyaml>=6.0

fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9

requests>=2.31      # 台股 FinMind
yfinance>=0.2.40    # 美股

# A 股。很重：實測拉進 32 個套件、site-packages 約 299MB，import 時吃
# 約 100MB RSS。用不到 A 股的話註解掉，建置會快很多、記憶體也寬鬆。
akshare>=1.14
