# 核心（必裝）
pandas>=2.0
pyyaml>=6.0

# 資料來源（按需安裝，只用哪個市場就裝哪個）
requests>=2.31      # 台股 FinMind
yfinance>=0.2.40    # 美股
akshare>=1.14       # A 股

# 開發
pytest>=8.0

# 網站版（可選；只想用 CLI 可以不裝）
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9

# 快取用 CSV，不需要 pyarrow。
